node_modules/
frontend/dist/
.env
.DS_Store
*.sqlite
screenshots/
test-results.json
playwright-report/
test-results/
firebase-credentials.json
scratch/
sessions/
videos/
adapters/*/finetune/.venv/
adapters/*/finetune/output/
adapters/*/finetune/**/*.safetensors
adapters/*/finetune/mlx-responses*.jsonl
__pycache__/
*.pyc
generated-scripts/

# Neměnný záznam auditů (D1–D3). Provozní důkazní materiál, ne zdrojový kód —
# do repozitáře nepatří a při klonování by se nesměl přepsat. Zálohuje se
# spolu s ostatními daty instalace.
ledger/
